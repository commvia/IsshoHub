# IsshoHub SSG 靜態生成 SEO 實現計劃

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推薦）或 superpowers:executing-plans 逐任務實現此計劃。步驟使用複選框（`- [ ]`）語法來跟蹤進度。

**目標：** 為每篇文章生成完整靜態 HTML，實現最強 SEO 配置，文章 URL 從 `/article/?slug=xxx` 改為 `/article/slug/`

**架構：** Node.js build script 在 Cloudflare Pages 部署時執行，從 Supabase 抓取所有已發布文章，逐一生成含完整 meta、JSON-LD 和預渲染內文的靜態 HTML 文件。互動功能（書籤、語言切換、相關文章）繼續由 client-side JS 處理。

**技術棧：** Node.js 20, @supabase/supabase-js v2, dotenv（開發用）, Cloudflare Pages Build, ES Modules

**規格文件：** `docs/superpowers/specs/2026-05-05-ssg-seo-design.md`

---

## 檔案結構

| 動作 | 路徑 | 職責 |
|------|------|------|
| 新增 | `package.json` | Node.js 項目，ESM，dependencies |
| 新增 | `.gitignore` | 排除 `node_modules/`、`.env` |
| 新增 | `build.js` | Build script：抓文章 → 填模板 → 寫靜態文件 → 生成 sitemap |
| 新增 | `article/_template.html` | 文章頁模板，`{{PLACEHOLDER}}` build 時替換 |
| 自動生成 | `article/[slug]/index.html` | Build 時生成，每篇文章一個，不手動編輯 |
| 自動生成 | `sitemap.xml` | Build 時重新生成，含所有文章 URL |
| 修改 | `article/index.html` | 相關文章連結格式更新 |
| 修改 | `assets/js/core.js` | `C.cardHTML()` 文章連結格式更新 |
| 修改 | `index.html` | `sbCardHTML()` 連結更新 + WebSite JSON-LD |
| 修改 | `bookmarks/index.html` | 文章連結格式更新 |
| 修改 | `admin/index.html` | 文章連結格式更新 |
| 修改 | `_redirects` | 移除 SPA article 攔截規則 |

---

## 任務 1：Node.js 項目初始化

**文件：**
- 新增：`package.json`
- 新增：`.gitignore`
- 新增：`.env`（本機用，不進 git）

- [ ] **步驟 1：創建 `package.json`**

路徑：`/Users/kit/Desktop/Isshohub/package.json`

```json
{
  "name": "isshohub",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.0"
  },
  "devDependencies": {
    "dotenv": "^16.4.0"
  }
}
```

- [ ] **步驟 2：創建 `.gitignore`**

路徑：`/Users/kit/Desktop/Isshohub/.gitignore`

```
node_modules/
.env
```

- [ ] **步驟 3：安裝 dependencies**

```bash
cd /Users/kit/Desktop/Isshohub && npm install
```

預期：出現 `node_modules/`，`package-lock.json` 生成

- [ ] **步驟 4：創建本機開發用 `.env`（不 commit）**

路徑：`/Users/kit/Desktop/Isshohub/.env`

```
SUPABASE_URL=https://eupqbbfbucdkhtpsuvry.supabase.co
SUPABASE_SERVICE_KEY=<從 Supabase Dashboard > Settings > API > service_role key 取得>
SITE_URL=https://isshohub.pages.dev
```

- [ ] **步驟 5：Commit（不包含 `.env`）**

```bash
git add package.json package-lock.json .gitignore
git commit -m "chore: 加入 Node.js build 環境"
git push
```

---

## 任務 2：創建文章頁模板 `article/_template.html`

**文件：**
- 新增：`article/_template.html`（從 `article/index.html` 改造）

**說明：** 把 `article/index.html` 複製一份為 `article/_template.html`，然後做以下 5 項修改。原本的 `article/index.html` 保持不動（作為訪問 `/article/` 無 slug 時的 fallback）。

- [ ] **步驟 1：複製文件**

```bash
cp /Users/kit/Desktop/Isshohub/article/index.html \
   /Users/kit/Desktop/Isshohub/article/_template.html
```

- [ ] **步驟 2：替換 `<title>` 和 `<meta description>`**

在 `article/_template.html` 找到（第 6-7 行）：
```html
<title>IsshoHub</title>
<meta name="description" content="" />
```

改為：
```html
<title>{{TITLE_TC}} — IsshoHub</title>
<meta name="description" content="{{EXCERPT_TC}}" />
```

- [ ] **步驟 3：在 `</head>` 前插入完整 SEO meta + JSON-LD**

在 `article/_template.html` 的 `</head>` 標籤之前插入（注意：不是 `</body>`）：

```html
<!-- SEO: pre-rendered at build time -->
<meta property="og:type" content="article" />
<meta property="og:site_name" content="IsshoHub" />
<meta property="og:url" content="{{CANONICAL_URL}}" />
<meta property="og:title" content="{{TITLE_TC}} — IsshoHub" />
<meta property="og:description" content="{{EXCERPT_TC}}" />
<meta property="og:image" content="{{OG_IMAGE}}" />
<meta property="og:image:width" content="1200" />
<meta property="og:image:height" content="630" />
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content="{{TITLE_TC}} — IsshoHub" />
<meta name="twitter:description" content="{{EXCERPT_TC}}" />
<meta name="twitter:image" content="{{OG_IMAGE}}" />
<link rel="canonical" href="{{CANONICAL_URL}}" />
<script type="application/ld+json">{{JSON_LD}}</script>
```

- [ ] **步驟 4：在 inline `<script>` 第一行前加入 slug 注入腳本**

在 `article/_template.html` 中，找到 `<script src="/assets/js/core.js` 的最後一個 `<script src>` 標籤，在它之後、inline `<script>(function()` 之前插入：

```html
<script>window.__ISSHO_SLUG = '{{SLUG}}';</script>
```

- [ ] **步驟 5：更新 slug 讀取邏輯**

在 `article/_template.html` 的 inline `<script>` 中找到：
```javascript
var slug = new URLSearchParams(window.location.search).get('slug') || '';
```

改為：
```javascript
var slug = window.__ISSHO_SLUG || new URLSearchParams(window.location.search).get('slug') || '';
```

- [ ] **步驟 6：更新 `articleUrl`（約第 431 行）**

找到：
```javascript
var articleUrl = 'https://isshohub.pages.dev/article/?slug=' + slug;
```

改為：
```javascript
var articleUrl = '{{CANONICAL_URL}}';
```

- [ ] **步驟 7：Commit**

```bash
git add article/_template.html
git commit -m "feat: 加入文章頁 SSG 模板"
git push
```

---

## 任務 3：創建 `build.js`

**文件：**
- 新增：`build.js`

**說明：** Build script 核心。讀 `.env`（本機）或環境變數（生產），從 Supabase 抓所有已發布文章，生成靜態 HTML 文件和 sitemap。

- [ ] **步驟 1：創建 `build.js` 頂部：import + 環境變數**

路徑：`/Users/kit/Desktop/Isshohub/build.js`

```javascript
// 本機開發讀取 .env（生產環境有真實環境變數）
try { const d = await import('dotenv'); d.config(); } catch {}

import { createClient } from '@supabase/supabase-js';
import fs from 'fs';
import path from 'path';

const BASE_URL     = (process.env.SITE_URL || 'https://isshohub.pages.dev').replace(/\/$/, '');
const SUPABASE_URL = process.env.SUPABASE_URL;
const SUPABASE_KEY = process.env.SUPABASE_SERVICE_KEY;

if (!SUPABASE_URL || !SUPABASE_KEY) {
  console.error('❌ 缺少環境變數 SUPABASE_URL 或 SUPABASE_SERVICE_KEY');
  process.exit(1);
}

const supabase = createClient(SUPABASE_URL, SUPABASE_KEY);
```

- [ ] **步驟 2：加入工具函數 `escHtml()`**

```javascript
function escHtml(str) {
  return String(str || '')
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;');
}
```

- [ ] **步驟 3：加入 Markdown parser `md()`**

把 `article/index.html` 第 281–355 行的 `function md(text) { ... }` **完整複製**到 `build.js`（這個函數無任何 DOM 依賴，純字串操作，在 Node.js 可直接使用）。

複製後驗證：函數開頭是 `function md(text) {`，結尾是第 355 行的 `}`。

- [ ] **步驟 4：加入靜態頁面列表（供 sitemap 使用）**

```javascript
const STATIC_PAGES = [
  { loc: `${BASE_URL}/`,         changefreq: 'daily',  priority: '1.0' },
  { loc: `${BASE_URL}/news/`,    changefreq: 'daily',  priority: '0.9' },
  { loc: `${BASE_URL}/visa/`,    changefreq: 'weekly', priority: '0.9' },
  { loc: `${BASE_URL}/biz/`,     changefreq: 'weekly', priority: '0.9' },
  { loc: `${BASE_URL}/house/`,   changefreq: 'weekly', priority: '0.9' },
  { loc: `${BASE_URL}/tax/`,     changefreq: 'weekly', priority: '0.9' },
  { loc: `${BASE_URL}/life/`,    changefreq: 'weekly', priority: '0.9' },
  { loc: `${BASE_URL}/culture/`, changefreq: 'weekly', priority: '0.8' },
  { loc: `${BASE_URL}/places/`,  changefreq: 'weekly', priority: '0.8' },
  { loc: `${BASE_URL}/pets/`,    changefreq: 'weekly', priority: '0.8' },
  { loc: `${BASE_URL}/story/`,   changefreq: 'weekly', priority: '0.8' },
];
```

- [ ] **步驟 5：加入 `generateSitemap()` 函數**

```javascript
function generateSitemap(articles) {
  const staticUrls = STATIC_PAGES.map(p =>
    `  <url>\n    <loc>${p.loc}</loc>\n    <changefreq>${p.changefreq}</changefreq>\n    <priority>${p.priority}</priority>\n  </url>`
  );

  const articleUrls = articles
    .filter(a => a.slug)
    .map(a => {
      const lastmod = (a.updated_at || a.published_at || '').substring(0, 10);
      return `  <url>\n    <loc>${BASE_URL}/article/${a.slug}/</loc>\n    <lastmod>${lastmod}</lastmod>\n    <changefreq>monthly</changefreq>\n    <priority>0.8</priority>\n  </url>`;
    });

  const xml = [
    '<?xml version="1.0" encoding="UTF-8"?>',
    '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">',
    '',
    ...staticUrls,
    '',
    ...articleUrls,
    '',
    '</urlset>',
    '',
  ].join('\n');

  fs.writeFileSync('sitemap.xml', xml, 'utf8');
  console.log(`✓ sitemap.xml 已更新（${STATIC_PAGES.length} 靜態頁 + ${articleUrls.length} 文章）`);
}
```

- [ ] **步驟 6：加入主 `build()` 函數**

```javascript
async function build() {
  const template = fs.readFileSync('article/_template.html', 'utf8');

  const { data: articles, error } = await supabase
    .from('articles')
    .select('*')
    .eq('status', 'published')
    .order('published_at', { ascending: false });

  if (error) {
    console.error('❌ Supabase 錯誤:', error.message);
    process.exit(1);
  }

  let generated = 0;

  for (const a of articles) {
    const slug = a.slug;
    if (!slug) continue;

    const canonicalUrl = `${BASE_URL}/article/${slug}/`;
    const titleTc    = a.title_tc   || a.title_en   || '';
    const titleEn    = a.title_en   || a.title_tc   || '';
    const excerptTc  = a.excerpt_tc || '';
    const excerptEn  = a.excerpt_en || '';
    const bodyTcHtml = md(a.body_tc || '');
    const bodyEnHtml = md(a.body_en || '');
    const ogImg      = a.cover_image_url
      || 'https://images.unsplash.com/photo-1540959733332-eab4deabeeaf?w=1200&q=80';
    const published  = (a.published_at || '').substring(0, 10);
    const modified   = (a.updated_at  || a.published_at || '').substring(0, 10);

    const jsonLd = JSON.stringify({
      '@context':    'https://schema.org',
      '@type':       'Article',
      'headline':    titleTc || titleEn,
      'description': excerptTc || excerptEn || titleTc,
      'image':       ogImg,
      'datePublished': published,
      'dateModified':  modified,
      'author':    { '@type': 'Organization', 'name': 'IsshoHub' },
      'publisher': { '@type': 'Organization', 'name': 'IsshoHub', 'url': BASE_URL },
      'url':         canonicalUrl,
    });

    // 注意：使用函數形式的 replace 避免 $ 符號在替換字串中的特殊行為
    let html = template
      .replace(/\{\{SLUG\}\}/g,          () => slug)
      .replace(/\{\{TITLE_TC\}\}/g,      () => escHtml(titleTc))
      .replace(/\{\{TITLE_EN\}\}/g,      () => escHtml(titleEn))
      .replace(/\{\{EXCERPT_TC\}\}/g,    () => escHtml(excerptTc))
      .replace(/\{\{EXCERPT_EN\}\}/g,    () => escHtml(excerptEn))
      .replace(/\{\{CANONICAL_URL\}\}/g, () => canonicalUrl)
      .replace(/\{\{OG_IMAGE\}\}/g,      () => ogImg)
      .replace(/\{\{JSON_LD\}\}/g,       () => jsonLd)
      .replace(/\{\{BODY_TC\}\}/g,       () => bodyTcHtml)
      .replace(/\{\{BODY_EN\}\}/g,       () => bodyEnHtml)
      .replace(/\{\{PUBLISHED_DATE\}\}/g,() => published)
      .replace(/\{\{AUTHOR\}\}/g,        () => escHtml(a.author || ''));

    const dir = path.join('article', slug);
    fs.mkdirSync(dir, { recursive: true });
    fs.writeFileSync(path.join(dir, 'index.html'), html, 'utf8');
    generated++;
  }

  generateSitemap(articles);
  console.log(`✓ 生成 ${generated} 篇文章靜態頁`);
}

build().catch(err => { console.error('❌ Build 失敗:', err); process.exit(1); });
```

- [ ] **步驟 7：本機測試**

確保 `.env` 已填入正確的 `SUPABASE_SERVICE_KEY`（從 Supabase Dashboard → Settings → API → service_role key），然後執行：

```bash
cd /Users/kit/Desktop/Isshohub && node build.js
```

預期輸出：
```
✓ 生成 N 篇文章靜態頁
✓ sitemap.xml 已更新（11 靜態頁 + N 文章）
```

確認：
1. `article/[某個slug]/index.html` 已生成
2. 打開該文件，確認 `<title>` 有文章標題（不是空的）
3. 確認有 `<script type="application/ld+json">` 且內容包含 `"@type": "Article"`
4. 確認 `sitemap.xml` 包含 `<loc>.../article/[slug]/</loc>` 條目

- [ ] **步驟 8：Commit**

```bash
git add build.js
git commit -m "feat: 加入 SSG build script"
git push
```

---

## 任務 4：更新所有內部文章連結格式

**說明：** 把所有 `/article/?slug=` 連結改為 `/article/slug/` 格式。涉及 5 個文件的共 8 處。

**文件：**
- 修改：`assets/js/core.js`（1 處）
- 修改：`index.html`（4 處）
- 修改：`article/index.html`（1 處）
- 修改：`bookmarks/index.html`（2 處）
- 修改：`admin/index.html`（1 處）

- [ ] **步驟 1：`assets/js/core.js`（第 32 行附近）**

找到：
```javascript
const articleUrl = `/article/?slug=${a.slug}`;
```
改為：
```javascript
const articleUrl = `/article/${a.slug}/`;
```

- [ ] **步驟 2：`index.html` — hero 連結（第 531 行附近）**

找到：
```javascript
const heroUrl = _sbHeroArticle ? `/article/?slug=${_sbHeroArticle.slug}` : null;
```
改為：
```javascript
const heroUrl = _sbHeroArticle ? `/article/${_sbHeroArticle.slug}/` : null;
```

- [ ] **步驟 3：`index.html` — side card 連結（第 568 行附近）**

找到：
```javascript
return `<a class="side-card" href="/article/?slug=${a.slug}">
```
改為：
```javascript
return `<a class="side-card" href="/article/${a.slug}/">
```

- [ ] **步驟 4：`index.html` — sbCardHTML url（第 653 行附近）**

找到：
```javascript
const url = `/article/?slug=${a.slug}`;
```
改為：
```javascript
const url = `/article/${a.slug}/`;
```

- [ ] **步驟 5：`index.html` — edit chip 連結（第 658 行附近）**

找到：
```javascript
<a class="card-edit-chip" href="/article/?slug=${a.slug}&edit=1"
```
改為：
```javascript
<a class="card-edit-chip" href="/article/${a.slug}/?edit=1"
```

- [ ] **步驟 6：`article/index.html` — 相關文章卡片（第 363 行附近）**

找到：
```javascript
return '<a class="card-compact" href="/article/?slug='+a.slug+'" style="text-decoration:none;">'
```
改為：
```javascript
return '<a class="card-compact" href="/article/'+a.slug+'/" style="text-decoration:none;">'
```

- [ ] **步驟 7：`bookmarks/index.html`（第 221、225 行附近）**

找到：
```javascript
+ '<a class="bm-card-media" href="/article/?slug=' + a.slug + '"'
```
改為：
```javascript
+ '<a class="bm-card-media" href="/article/' + a.slug + '/"'
```

找到：
```javascript
+ '<a class="bm-card-title" href="/article/?slug=' + a.slug + '">' + title + '</a>'
```
改為：
```javascript
+ '<a class="bm-card-title" href="/article/' + a.slug + '/">' + title + '</a>'
```

- [ ] **步驟 8：`admin/index.html`（第 248 行附近）**

找到：
```javascript
(a.slug?'<a class="adm-btn" href="/article/?slug='+a.slug+'" target="_blank">👁 預覽</a>':'')
```
改為：
```javascript
(a.slug?'<a class="adm-btn" href="/article/'+a.slug+'/" target="_blank">👁 預覽</a>':'')
```

- [ ] **步驟 9：Commit**

```bash
git add assets/js/core.js index.html article/index.html bookmarks/index.html admin/index.html
git commit -m "refactor: 文章連結格式從 ?slug= 改為 /article/slug/"
git push
```

---

## 任務 5：更新 `_redirects`

**文件：**
- 修改：`_redirects`

- [ ] **步驟 1：移除 article SPA redirect**

打開 `/Users/kit/Desktop/Isshohub/_redirects`，找到並刪除這兩行：
```
# Article detail pages
/article/*  /article/index.html  200
```

刪除後確認文件其餘內容保持不變（trailing-slash redirects 等）。

- [ ] **步驟 2：Commit**

```bash
git add _redirects
git commit -m "fix: 移除 SPA article redirect，靜態文件優先"
git push
```

---

## 任務 6：主頁加入 WebSite + Organization JSON-LD

**文件：**
- 修改：`index.html`

- [ ] **步驟 1：在 `index.html` 的 `</head>` 前加入 JSON-LD**

找到 `index.html` 的 `</head>` 標籤，在之前插入：

```html
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "WebSite",
      "@id": "https://isshohub.pages.dev/#website",
      "url": "https://isshohub.pages.dev/",
      "name": "IsshoHub",
      "description": "繁體中文與英文雙語資訊平台，專為在日本生活或準備移居日本的外國人而設。",
      "inLanguage": ["zh-Hant", "en"]
    },
    {
      "@type": "Organization",
      "@id": "https://isshohub.pages.dev/#organization",
      "name": "IsshoHub",
      "url": "https://isshohub.pages.dev/",
      "description": "在日外國人的繁中英雙語資訊平台"
    }
  ]
}
</script>
```

- [ ] **步驟 2：Commit**

```bash
git add index.html
git commit -m "feat: 主頁加入 WebSite + Organization JSON-LD"
git push
```

---

## 任務 7：配置 Cloudflare Pages Build（手動操作）

**說明：** 在 Cloudflare Pages Dashboard 手動操作，不涉及代碼。

- [ ] **步驟 1：取得 Supabase service_role key**

前往 [Supabase Dashboard](https://supabase.com/dashboard) →選 IsshoHub 項目 → Settings → API → 複製 **service_role** key（不是 anon key，兩者勿混淆）

- [ ] **步驟 2：設定 Cloudflare Pages Build**

前往 [Cloudflare Pages Dashboard](https://dash.cloudflare.com) → IsshoHub 項目 → Settings → Build & deployments，設定：

| 設定項 | 值 |
|--------|-----|
| Build command | `npm install && node build.js` |
| Build output directory | 留空（部署整個 repo 根目錄） |
| Node.js version | `20` |

- [ ] **步驟 3：設定 Production 環境變數**

在 Settings → Environment variables → **Production**，加入以下 3 個變數：

| 變數名 | 值 |
|--------|-----|
| `SUPABASE_URL` | `https://eupqbbfbucdkhtpsuvry.supabase.co` |
| `SUPABASE_SERVICE_KEY` | （步驟 1 複製的 service_role key） |
| `SITE_URL` | `https://isshohub.pages.dev` |

- [ ] **步驟 4：觸發 build 並確認 log**

推一個空 commit 觸發 build：
```bash
git commit --allow-empty -m "chore: 觸發 Cloudflare Pages SSG build"
git push
```

在 Cloudflare Pages → Deployments 頁面確認 Build log 出現：
```
✓ 生成 N 篇文章靜態頁
✓ sitemap.xml 已更新（11 靜態頁 + N 文章）
```

- [ ] **步驟 5：端對端驗證**

Build 完成後，用瀏覽器訪問任一文章的新 URL（從 Cloudflare Pages Deployments → 部署 URL → 點任一文章卡片）。

然後在瀏覽器按 **Ctrl+U（或 Cmd+U）查看原始碼**，確認：
1. `<title>` 有實際文章標題（不是 `IsshoHub`）
2. `<meta name="description">` 有文章摘要
3. 有 `<script type="application/ld+json">` 且包含 `"@type":"Article"`
4. 有 `<link rel="canonical" href="https://isshohub.pages.dev/article/[slug]/">`
5. 訪問 `https://isshohub.pages.dev/sitemap.xml`，確認有文章 URL 條目

---

## 驗收標準

全部任務完成後，滿足以下條件即為完成：

- [ ] `node build.js` 在本機執行成功，無錯誤
- [ ] 每篇已發布文章在 `article/[slug]/index.html` 有對應靜態文件
- [ ] 任一文章頁的原始碼有完整 `<title>`、`<meta description>`、OG tags、canonical、JSON-LD
- [ ] `sitemap.xml` 包含所有文章 URL
- [ ] 所有文章卡片連結指向 `/article/slug/` 格式（確認主頁、分類頁、書籤頁）
- [ ] Cloudflare Pages build log 顯示成功生成
- [ ] 主頁原始碼有 `WebSite` + `Organization` JSON-LD

---

## 日後：域名更換時的操作

購買域名並在 Cloudflare Pages 設定 Custom Domain 後：

1. 在 Cloudflare Pages Environment variables 把 `SITE_URL` 改為新域名（如 `https://isshohub.com`）
2. 在 `index.html` 的 canonical 和 JSON-LD 把 `isshohub.pages.dev` 改為新域名
3. Redeploy 一次
